export enum GqlBlockType {
  scalar = 'scalar',
  type = 'type',
  input = 'input',
  enum = 'enum',
}

export enum GqlType {
  Int = 'Int',
  Float = 'Float',
  String = 'String',
  Boolean = 'Boolean',
  ID = 'ID',
}
